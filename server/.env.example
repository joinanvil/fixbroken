# GitHub OAuth App credentials
# Create one at: https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Secret used to sign JWTs — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

# Where the frontend is hosted (no trailing slash)
WEBAPP_URL=https://joinanvil.ai

# This server's public URL (no trailing slash) — set in Vercel dashboard
SERVER_URL=https://api.joinanvil.ai
